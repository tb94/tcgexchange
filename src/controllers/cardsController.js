// src/controllers/cardsController.js

// Placeholder for adding a card to the user's collection
exports.addToCollection = (req, res) => {
  res.status(200).json({ message: 'Card added to collection (placeholder)' });
};

// Placeholder for adding a card to the user's wishlist
exports.addToWishlist = (req, res) => {
  res.status(200).json({ message: 'Card added to wishlist (placeholder)' });
};
